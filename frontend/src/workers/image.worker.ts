type T={file:File;mode:"target"|"resize";targetBytes?:number;width?:number;height?:number;outputType?:string;requestId?:number};const w=self as unknown as {onmessage:((e:MessageEvent<T>)=>void)|null;postMessage:(x:unknown)=>void};const enc=(c:OffscreenCanvas,t:string,q:number)=>c.convertToBlob({type:t,quality:q});w.onmessage=async({data})=>{let stage="decode";try{const b=await createImageBitmap(data.file);stage="canvas";if(typeof OffscreenCanvas==="undefined")throw Error();const width=data.mode==="resize"?data.width!:b.width,height=data.mode==="resize"?data.height!:b.height,c=new OffscreenCanvas(width,height),ctx=c.getContext("2d",{alpha:true});if(!ctx)throw Error();ctx.drawImage(b,0,0,width,height);b.close();stage="encode";const type=data.outputType||(data.file.type==="image/png"?"image/webp":data.file.type);let blob:Blob,minimumSize:number|undefined;if(data.mode==="resize")blob=await enc(c,type,.92);else{const target=data.targetBytes!,smallest=await enc(c,type,.05);minimumSize=smallest.size;let low=.05,high=.98,best:Blob|undefined;for(let i=0;i<9;i++){const q=(low+high)/2,candidate=await enc(c,type,q);if(candidate.size<=target){best=candidate;low=q}else high=q}blob=best||smallest}w.postMessage({ok:true,blob,width,height,type,minimumSize,requestId:data.requestId})}catch(error){console.warn("Image worker failed",stage,error);w.postMessage({ok:false,requestId:data.requestId,error:{code:stage==="decode"?"DECODE_FAILED":stage==="canvas"?"CANVAS_FAILED":"ENCODE_FAILED",stage}})}}