export type ProcessingError={code:string;stage:string}; export type ProcessResult={blob:Blob;width:number;height:number;type:string;minimumSize?:number;requestId?:number}; export function processInWorker(payload:Record<string,unknown>):Promise<ProcessResult>{return new Promise((resolve,reject)=>{let worker:Worker;try{worker=new Worker(new URL("../workers/image.worker.ts",import.meta.url))}catch{reject({code:"WORKER_FAILED",stage:"unknown"});return}const done=()=>worker.terminate();worker.onmessage=({data})=>{done();data.ok?resolve(data):reject(data.error)};worker.onerror=()=>{done();reject({code:"WORKER_FAILED",stage:"unknown"})};worker.postMessage(payload)})}